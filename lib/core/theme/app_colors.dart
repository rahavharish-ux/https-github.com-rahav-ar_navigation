import 'package:flutter/material.dart';

/// Seed and semantic colors not already covered by [ColorScheme.fromSeed].
class AppColors {
  const AppColors._();

  static const Color seed = Color(0xFF0B5FFF);
  static const Color success = Color(0xFF1E8E3E);
  static const Color warning = Color(0xFFF9A825);
}
